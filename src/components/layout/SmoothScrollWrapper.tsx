import { type ReactNode } from 'react'

/** Native scroll — lighter than Lenis on mobile and desktop */
export function SmoothScrollWrapper({ children }: { children: ReactNode }) {
  return <>{children}</>
}
